package com.tf2center.discordbot.publish;

import java.util.List;

public final class PublishersRefresher  {

//    DefaultSingletonBeanRegistry registry = (DefaultSingletonBeanRegistry) applicationContext.getAutowireCapableBeanFactory();
//    registry.destroySingleton("ConfigManager");
//    registry.registerSingleton("ConfigManager", new ConfigManager(filePath));

//    private final List<Publishable> publishables = new ArrayList<Publishable>();
//    private static ApplicationContext ctx;
//    private PublishersRefresher() {}
//
//

//

    public static List<Publishable> refresh(){
        return null;
    }

//    @Override
//    public void setApplicationContext(ApplicationContext applicationContext) throws BeansException {
//        context = applicationContext;
//        List<Object> publishableBeans = Arrays.stream(applicationContext.getBeanDefinitionNames())
//                .filter(beanName -> beanName.contains("Publishable"))
//                .map(applicationContext::getBean)
//                .toList();
//
//        publishableBeans.forEach(bean ->
//                publishables.add((LobbyPublishable) bean)
//        );
//    }



//    public List<Publishable> getPublishables() {
//        return publishables;
//    }
}
